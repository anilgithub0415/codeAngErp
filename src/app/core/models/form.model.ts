export interface form {
  
    tenantId:string;
    formKey: string;
    formlyConfig: string; 
   
  
}

export interface createFormDto {
  
    tenantId:string;
    formKey: string;
    formlyConfig: string; 
   
  
}

