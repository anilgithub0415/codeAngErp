import { StudentQuestionAnswer } from './student-question-answer';

describe('StudentQuestionAnswer', () => {
  it('should create an instance', () => {
    expect(new StudentQuestionAnswer()).toBeTruthy();
  });
});
