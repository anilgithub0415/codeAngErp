import { DeleteTooltipDirective } from './delete-tooltip.directive';

describe('DeleteTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
