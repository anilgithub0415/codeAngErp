* While initialising Enrollment may be by AdmissionsOfficer/InstituteAdmin we need 
    1. from people list , for each person record we need Enroll/Add to program button 
    2. from student list the same we need ( ithink we need seperate Student Screen)
    3. Officer clicks 'New Enrollment' button and navigates to Enrollment screen, where he searches Person and if not found Creates New and go ahead 
    4. Coordinator may manage enrollments for specific depts or programs
    5. Less common - Admin might view a program there might be 'Enroll Student' button that takes him to enrollment with selected program 
    6.  Self assessment by student (with approval workflow) - Here 


    In enrollment we need to display below fields 
    contactEmail, contactPhone, StudentProfile.studentIdNumber , Program.programName, programCode, description, 
    Enrollment Date: Default to current date, but editable,
    Enrollment Status: (e.g., 'Active', 'Pending Approval', 'Provisional', 'Withdrawn').
    Payment Status/Details (For Admin roles): (If your system handles this, e.g., 'Paid', 'Partial Payment', 'Pending').
    Notes/Comments: Any administrative notes.

    Should display all course names , codes from selected program.

    (Optional) We need to display Course Offering/Batch Assignment 
            For each course in the program, a dropdown or selection mechanism to assign the student to a 
            specific CourseOffering (batch). This is where the student gets assigned to a specific class with a specific faculty and schedule.

